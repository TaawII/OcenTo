doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Logowanie
    link(rel="stylesheet", href="/stylesheets/register/style.css")
  body
    .container
      .form-section
        form(action="/login", method="POST")
          h1 Zaloguj się
          if success
            p.success-message(style="color: green; margin-bottom: 10px;")= success
          input(type="text", name="username", placeholder="Nazwa użytkownika", required)
          input(type="password", name="password", placeholder="Hasło", required)
          if error
            .error-message(style="display:block; color:red; margin-top:10px;")
              span= error
          button.button Zaloguj się
          p(style="margin-top: 1rem; text-align: center;")
            | Jeżeli nie posiadasz konta 
            a(href="/register", style="color: #6c63ff; text-decoration: none; font-weight: 600;") zarejestruj się
      .graphic-section
